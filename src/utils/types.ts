export enum Sides {
  Defense = 'defense',
  Attack = 'attack'
}

export enum GameState {
  Waiting = 'waiting',
  Rolling = 'rolling',
  Finished = 'finished'
}

export enum Direction {
  Up = 'up',
  Down = 'down'
}