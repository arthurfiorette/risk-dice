export enum Sides {
  Attack = 'attack',
  Defense = 'defense'
}

export enum GameState {
  Waiting = 'waiting',
  Rolling = 'rolling',
  Finished = 'finished'
}

export enum Direction {
  Up = 'up',
  Down = 'down'
}
